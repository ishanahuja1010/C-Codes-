#include <stdio.h>
#include<string.h>
int main()
{
    char s[100],g[100],f[100];
    int a,i,j,c=0,d,e=0,b;
    gets(s);
    a=strlen(s);
   for(i=0;i<a;i++)
   {
       b=s[i+1]-s[i];
       if(b!=1)
       c++;
   }
   if(c==1)
   printf("yes");
   else
   printf("no");    
}
